/// A class representing the Navigation Rail Information.
class NavigationRailModel{
  final String toggleImage;
  final String? label;
  final String image;
  final String route;
  final int selectedIndex;
  final int index;
/// Constructor for creating a [NavigationRailModel] instance.
  const NavigationRailModel({required this.toggleImage, required this.label,required this.image ,required this.route, required this.selectedIndex, required this.index, });
}